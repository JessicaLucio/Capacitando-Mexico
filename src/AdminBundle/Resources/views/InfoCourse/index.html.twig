{% extends "WebBundle::layout.html.twig" %}

{% block content %}

    <div class='row'>
        <div class='col-xs-12'>

            <div class="wrapper-contenido">
                <h1 class="subtitle">Informacion del curso</h1>
                <hr>
                <a href="{{path('info_course_add')}}" class='btn btn-success m-xs-r-2'><i class='fa fa-plus m-xs-r-1'></i>Añadir Informacion Curso</a>
                <hr>
                {% for message in app.session.flashBag().get('status') %}
                    <div class="alert alert-success alert-dismissible alert-js" role="alert">
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                        {{ message }}
                    </div>
                {% endfor %}
            </div>

        </div>
        <div class='col-xs-12'>
            <div class='table-responsive'>
                <table class="table table-striped table-bordered table-alpha" cellspacing="0" width="100%" id="myTable">
                    <thead>
                        <tr style="text-align: center;">
                            <th>Contenido</th>
                            <th>Curso</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for info_course in info_courses %}
                            <tr>
                                <td>{{ info_course.body|slice(0, 150) }}</td>
                                <td>{{ info_course.course.name }}</td>
                                <td class="text-center ico-actions">
                                    <a href="{{ path('info_course_edit', {'id':info_course.id}) }}" class="ico-pencil m-xs-r-1">
                                        <span class="fa fa-pencil" aria-hidden="true"></span>
                                    </a> 
                                    <a href="{{ path('info_course_delete', {'id':info_course.id}) }}" class="ico-trash m-xs-r-1">
                                        <span class="fa fa-trash" aria-hidden="true"></span>
                                    </a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

{% endblock %}

